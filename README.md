# Dockerfiles

Go app to display dashboard of [my Dockerfiles](https://github.com/redjax/Dockerfiles). Refreshes the package feed on an interval and serves a simple HTML file template to display the data.
